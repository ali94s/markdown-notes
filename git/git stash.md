## git stash

```
git stash   暂存当前的修改
git stash pop	弹出当前stash栈顶的修改


git stash  可以暂存多次
git stash list	查看缓存的修改
git stash apply stash@{index} 应用某次缓存

git stash stash save "保存目前func的修改"		save来打标签，多次缓存后方便弹出
```

